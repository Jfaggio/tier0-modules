# tier0-modules - DEMO STUB

Folder structure only (`modules/<name>/<version>/`), so that the factory preflight can check that a
catalogue tag carries the module versions a building block pins. No module code: the real
catalogue belongs to Saint-Gobain and stays there. Tags mirror the real ones.
