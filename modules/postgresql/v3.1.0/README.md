# postgresql (DEMO)

Simplified stand-in for the Tier-0 catalogue module of the same name: same inputs and outputs, a
minimal implementation, none of the platform integrations (no RPZ refresh, no central Log
Analytics diagnostics, no PCS function call). For demonstrations on a personal tenant only.

Creates a Flexible Server B_Standard_B1ms: roughly 15 EUR/month while it exists.
